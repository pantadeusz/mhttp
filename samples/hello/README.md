# Hello world !!

This is starter project

## Build

```bash
mkdir -p build
cd build
cmake ..
make
cd ..
./build/webapidemo
```

And then type in web browser:
(http://localhost:5555)[http://localhost:5555]
